import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Spacing" />

export const Bar = ({ token, px }) => (
  <div style={{ display: 'flex', alignItems: 'center', gap: 16, padding: '4px 0' }}>
    <code style={{ fontSize: 12, width: 140, flex: 'none' }}>{token}</code>
    <div style={{ width: `var(${token})`, height: 14, background: 'var(--folio-color-accent)' }} />
    <span style={{ fontSize: 12, opacity: 0.6 }}>{px}</span>
  </div>
);

# Spacing

Échelle sur base 4px. Le blanc tournant fait partie du design : en cas de doute, prendre le cran au-dessus.

<Bar token="--folio-space-1" px="4px" />
<Bar token="--folio-space-2" px="8px" />
<Bar token="--folio-space-3" px="12px" />
<Bar token="--folio-space-4" px="16px" />
<Bar token="--folio-space-5" px="24px" />
<Bar token="--folio-space-6" px="32px" />
<Bar token="--folio-space-7" px="40px" />
<Bar token="--folio-space-8" px="48px" />
<Bar token="--folio-space-9" px="64px" />
<Bar token="--folio-space-10" px="96px" />

## Conventions

| Contexte | Token |
| --- | --- |
| Interstice d'un groupe de tags/chips | `--folio-space-2` |
| Gap entre boutons | `--folio-space-3` |
| Padding vertical des lignes de liste | `--folio-space-4` |
| Gap entre colonnes d'une grille de stats | `--folio-space-6` |
| Padding horizontal de page (desktop) | `--folio-space-8` |
| Espacement entre sections | `--folio-space-9`–`10` |

## Grille

- Contenu centré, largeur max de lecture ~720px, mesures de texte ≤ 60ch.
- Breakpoint principal : `--folio-breakpoint-mobile` (768px). Un seul breakpoint — les layouts doivent être fluides entre les deux.
- Le radius est unique et vaut zéro : `--folio-radius-none`.
