{
  "name": "@gcaillet/folio-ds",
  "version": "0.2.0",
  "description": "Folio DS — Guillaume Caillet's design system. Swiss-style web components built with Lit.",
  "type": "module",
  "license": "MIT",
  "author": "Guillaume Caillet",
  "main": "./dist/folio-ds.js",
  "module": "./dist/folio-ds.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/folio-ds.js",
    "./tokens.css": "./dist/tokens.css"
  },
  "files": [
    "dist",
    "custom-elements.json"
  ],
  "scripts": {
    "tokens": "node scripts/build-tokens.mjs",
    "dev": "npm run tokens && vite",
    "build": "npm run tokens && npm run analyze && vite build && tsc --emitDeclarationOnly && cp src/styles/tokens.css dist/tokens.css",
    "analyze": "cem analyze --config cem.config.mjs",
    "storybook": "npm run tokens && npm run analyze && storybook dev -p 6006",
    "build-storybook": "npm run tokens && npm run analyze && storybook build"
  },
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "@custom-elements-manifest/analyzer": "^0.10.4",
    "@storybook/addon-docs": "^9.1.5",
    "@storybook/web-components-vite": "^9.1.5",
    "storybook": "^9.1.5",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  },
  "customElements": "custom-elements.json"
}
